#! specs.yaml
name: zero2prod
region: fra1
services:
  - name: zero2prod
    dockerfile_path: dockerfile
    github:
      repo: shljonki/zero2prod
      branch: main
      deploy_on_push: true
    source_dir: .
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
    
